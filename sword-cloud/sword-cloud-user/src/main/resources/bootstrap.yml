spring:
  cloud:
    config:
      uri: http://localhost:3344
      label: master
      name: user-center
      profile: dev
      #discovery:
        #enabled: true
        #service-id: config-center
      #fail-fast: true