server:
  port: 7001
spring:
  application:
    name: config-center
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configs/dev
          #searchLocations: file:/d:/configs/{profile}
        #git:
          #uri: https://github.com/spring-cloud-samples/config-repo
          #default-label: master
          #force-pull: true
          #searchPaths: '{profile}'
          #basedir: sword-cloud/sword-cloud-config/src/main/resources/configs
          #username: 576260496@qq.com
          #password: w13132354113
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:6001/eureka/
    registry-fetch-interval-seconds: 5
  instance:
    lease-expiration-duration-in-seconds: 15
    lease-renewal-interval-in-seconds: 5
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}